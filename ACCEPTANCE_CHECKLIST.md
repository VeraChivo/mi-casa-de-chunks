# ✅ Feature Acceptance Checklist

工程驗收表，不是世界觀規則——命名/版式的判斷邏輯在 `CLAUDE.md`（🏷️命名與版式規則／🤝VERA協作模式），這份只管「動手前後要確認什麼」，兩者分開維護，不要把驗收清單的內容搬進CLAUDE.md，也不要把命名哲學寫進這裡。

## 新增/修改任何功能前

- [ ] 這是 Bug／UX／教學／命名／資料問題？（決定用哪一種解法，避免用工程解法處理教學問題）
- [ ] 目前實際狀態？（Reality Check：讀程式碼/畫面，不是讀待辦清單就下判斷）
- [ ] 是否已有類似機制？（避免重造一個已存在的功能）
- [ ] 是否有重複名稱？（同一個東西是否已經有別的名字在別處出現，見CLAUDE.md名稱層級規則）
- [ ] 是否有入口→展開→深入層級？（正式術語/詳細規則不該出現在入口層）
- [ ] 修改後是否破壞原本行為？（尤其共用的render函數/CSS class，改一處要查有沒有其他地方在用）

## 修改後

- [ ] Playwright驗證哪些情境？（正常情境＋至少一個邊界情境，不能只看程式碼邏輯就假設沒問題）
- [ ] `node --check` + `maintenance.js` 是否0錯誤？
- [ ] `index.html` 裡對應的 `?v=` 版本號是否同步跳號？
- [ ] 文件狀態是否同步？（CLAUDE.md有沒有需要更新的過時記錄，或這次修改本身該不該留一筆記錄）

## 這份checklist想防的三類重工（2026-07-26 建立時的實際案例）

1. **完成紀錄與實際狀態不同**：g75/g76/g77已完成但待辦沒更新、音檔清單已完成但仍顯示未開始 → 對應「目前實際狀態？」
2. **修改沒有先盤全域影響**：賀詞從單句→拆句問題、人物冊副標變四行造成版面退化、隨機賀詞與能量提示混淆 → 對應「是否有類似機制？」「修改後是否破壞原本行為？」
3. **局部修正破壞既有體驗**：修一個問題時沒確認是否影響其他入口 → 對應「修改後是否破壞原本行為？」「Playwright驗證哪些情境？」
