# 🌱 巡園紀錄（UX_ISSUES.md）

VERA 平常逛莊園時隨手記的小卡頓，不用停下來修、不用寫技術細節。累積 5～10 筆再一次交給工程端收斂。

這是一個「問題停車場」——先把問題停在這裡，避免「今天想到、改一半、忘記原本為什麼改」。
不是改功能，只是先把發現的東西記下來。

**格式（每筆固定五項）：**

```
### 📍 位置
（在哪個分頁/哪張卡）

問題：
（客觀描述哪裡怪怪的/卡住了）

第一次使用者可能誤會：
（猜猜看新來的人會怎麼誤解這裡，沒有頭緒也可以留空）

方向：
（有想法就寫，沒有也沒關係）

狀態：
待修 / 已修（日期）
```

---

## 待處理

分兩區記，避免真bug跟體驗調整混在一起：🔴真bug＝明確錯誤（連結死掉/資料對不上/畫面卡住），一發現就照CLAUDE.md規則立即修；🟡UX改善候選＝功能本身沒壞，只是呈現方式/資訊量值得再想想，先停在這裡等產品決策，不代表要馬上動工。

### 🔴 真bug

（目前沒有紀錄）

### 🟡 UX改善候選

#### 📍 全站（不是單一分頁）· 文字對比度

問題：
用 Playwright 實測全站 4 分頁、所有區塊展開後的所有可見文字，套 WCAG AA 標準
（一般文字需 4.5:1、18px以上粗體大字需 3:1），比對「真正的有效背景色」（含
`linear-gradient`，不是只看 `background-color`）。掃出 16 種文字/背景組合沒
達到 AA 標準，大多集中在「白字疊在中亮度色塊」跟「淺灰藍字疊在淺色底」這兩類：

| 元素（class） | 實際用途 | 對比度 | 需要 |
|---|---|---|---|
| `.pb-book-title`／`.pb-book-sub`（ammo-book-top） | 🌱新人路線圖／📦入倉語塊等收合卡標題列（白字疊水色漸層） | 1.93:1 | 4.5:1 |
| `▼ 展開` 收合箭頭（同上標題列內） | 1.93:1 | 4.5:1 |
| `.main-tab-btn.active` | 四個主分頁選中狀態（白字疊 --misora） | 2.25:1 | 4.5:1 |
| `.ep-chip.active` | 集數選單選中狀態（同上顏色） | 2.25:1 | 4.5:1 |
| `.ep-chip`（未選中） | 集數選單一般狀態（--misora字疊白底） | 2.25:1 | 4.5:1 |
| `.nav-btn` | 「‹上一句／下一句›」按鈕文字 | 2.25:1 | 4.5:1 |
| `.nav-count` | 「1 / 10」進度數字 | 2.25:1 | 4.5:1 |
| `.mask-toggle-btn` | 「✨揭露原釀·偷吃步」按鈕文字 | 2.44:1 | 4.5:1 |
| `.header-start-btn` | Header「🌱點播初芽」按鈕 | 2.52:1 | 4.5:1 |
| `.ms-next-hint` | 「再4粒→下一階」小提示 | 2.32:1 | 4.5:1 |
| `.cog-unlock-badge` | 「💡揭開原釀秘方」徽章 | 2.91:1 | 4.5:1 |
| `.v-word-light` | SVO配色系統裡動詞淺色語塊（如"gusta"） | 3.45:1 | 4.5:1 |
| `.pb-title-sep`／`.full-sent` | 精釀程序分隔符／整句朗讀框 | 4.11–4.37:1 | 4.5:1（僅些微差距，接近達標） |

第一次使用者可能誤會：
不會誤會功能壞掉——這批全部是「看得到但比較費眼」，不是看不到。在手機戶外強光、
或視力不如年輕人的使用情境下，這些低對比文字（尤其是分頁按鈕/上下句按鈕/進度數字
這幾個高互動頻率的控制項）會比較吃力，可能被誤以為是禁用狀態（灰階看起來像disabled）。

方向（純數據記錄，不是修改建議——顏色屬於VERA的水色系審美偏好，AI不自行調色）：
- `.v-word-light`／`.pb-title-sep`／`.full-sent` 屬於語意配色系統（SVO語塊/裝飾分隔符），
  本來就不一定要照純文字閱讀的AA標準看待，優先度最低
- 真正影響操作的是分頁/按鈕/進度數字這幾個高頻互動控制項（`.main-tab-btn.active`／
  `.ep-chip`／`.nav-btn`／`.nav-count`／`.mask-toggle-btn`／`.header-start-btn`），
  這幾個如果要調，最小改法通常是文字加粗＋加一層文字陰影，或背景色加深一階，
  不用整套水色系重新設計
- `.pb-book-title`／`.pb-book-sub`（白字疊水色漸層）是最低分的一組，同一個class
  在📦入倉/🌱路線圖/🌾莊園巡禮三處收合標題列共用，改一處會連動全部

狀態：
🟡 待決策（2026-08-20記錄）——純數據盤查，未動任何顏色，等VERA判斷要不要調、
調到什麼程度


#### 📍 ☀️日光育苗場 · 世界新聞（🌟本週推薦，Phase 2，未做）

問題：
一打開「世界新聞」直接展開全部 53 篇新聞，資訊量過大——VERA已定案三層架構（🌟本週推薦2篇→🔍主題探索→📚全部新聞53篇），Phase 1（主題探索收起）已完成（見已收斂），這裡剩Phase 2「本週推薦」還沒做。

第一次使用者可能誤會：
不知道從哪裡開始看，容易覺得這是一整個要讀完的資料庫，而不是「有空再挑幾篇看看」的地方。

方向（VERA已定調不是單純UI微調，是產品策略決定，先不動手）：
Phase 2「🌟本週推薦」需要先決定：
1. 推薦依什麼產生？隨機／週期輪替／人工策展挑選？
2. 是否依學習進度個人化？（VERA已排除，會牽涉閱讀紀錄/興趣標籤/推薦演算法/儲存狀態，現階段太複雜）
3. 是否留給未來付費功能？（VERA已排除，現在的問題不是缺高級內容，是入口不清楚，先不綁付費）
建議方向是「半人工輪替」：每週固定1篇時事＋1篇生活文化，比例像老師幫學生挑教材，不是完全隨機也不是演算法推薦。

狀態：
🟡 待決策（2026-08-04）——Phase 1已完成上線，Phase 2需要先確定資料結構/輪替機制才能動工，等VERA排時間再討論

---

## 已收斂

### 📍 ☀️日光育苗場 · 🫐遍地捻野莓（同源詞中文被裁掉）

問題：
`.cog-zh{white-space:nowrap}` 讓同源詞列表的中文欄位不能換行，長字串會撐出容器、
被全站的 `overflow-x:hidden` 直接裁掉。實測 COGNATE_LIBRARY 38 筆中有 4 筆受影響
（germane 右緣 479px／query 470px／account 563px／profound 410px，viewport 只有 390px）。

方向：
照 VERA 已排好的優先順序做第一項——拿掉 `white-space:nowrap` 改成正常換行，文案一個字不動。
（第 2、3 項「改成上下兩行」「縮短文案」都沒有用到，內容完整度維持原樣。）

狀態：
✅ 已修（2026-08-20）——`.cog-zh` 改 `min-width:0;overflow-wrap:anywhere`（style.css）。
額外加一條 `.cog-row>.cog-es{flex:1 0 auto}`：第一版修完發現 account 那行的中文變長之後，
反過來把西語 `el cuento` 擠成兩行——西語是這區的主要學習內容，優先保持完整一行。
已用 Playwright 在 390px 實測：38 行 0 溢出、4 行正常換行（germane/query/account/profound）、
西語欄維持單行、頁面 scrollWidth 仍是 390、前後綴與假朋友區塊（共用 `.cog-zh`）未受影響。

### 📍 ☀️日光育苗場 · 世界新聞（主題探索Phase 1：chip收合＋左右捲動修正）

問題：
主題篩選chip原本13個一次攤開、左右捲動（`overflow-x:auto`），VERA提出三層架構定案（🌟本週推薦→🔍主題探索→📚全部新聞），這裡是Phase 1「主題探索」，不含Phase 2「本週推薦」（見上方待處理，需要另外的資料結構/輪替機制決定，先不做）。

方向：
第一層只留「全部」+目前選中的主題（若不是全部）+「🔍查看全部主題」按鈕；展開後其餘主題chip用`flex-wrap:wrap`自然排列，不再左右捲動。不新增新聞資料欄位、不改文章分類，純呈現方式調整。

狀態：
✅ 已修（2026-08-04）——`toggleNewsTopicList()`／`_newsTopicListOpen`（script.js），`.news-topic-filter-more`／`.news-topic-more-btn`（style.css）。已用Playwright驗證：預設只顯示全部(53)+查看全部主題／展開後12個主題wrap排列不捲動／選特定主題後該主題會留在第一層＋展開列排除重複／切回全部正確顯示53篇／收合展開來回切換正常，無錯誤。

### 📍 全站（不是單一分頁）

問題：
2026-07-19全站TTS盤查——比對`audio-manifest.js`各音檔對照表跟資料檔實際內容量，發現E11整集
（亡靈節特輯）完全沒錄音、mom.js第25-39句沒錄音、grammar.js有166句沒錄音，其餘（歌曲/新聞）
也大多是TTS。詳細清單見 `RECORDING_QUEUE.md`。

方向：
依`RECORDING_QUEUE.md`建議順序分批錄

狀態：
✅ 已修（2026-07-19）——四批全部錄完接上：文法儲水槽166句、E11整集59句(語塊+整句)、
mom.js 15句、歌曲25句+新聞53句，全站不再fallback TTS
